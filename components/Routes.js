export default class Routes {
    constructor(toXSelector, to10Selector, ansSelector) {
        this._toXSelector = toXSelector;
        this._to10 = to10Selector;
        this._ansSelector = ansSelector;
    };
    open (selector) {
        
    }
}